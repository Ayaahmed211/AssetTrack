package com.assettrack.backend.domain;

/**
 * Represents the physical category of a hardware asset.
 * Used for filtering, reporting, and spare-laptop logic.
 */
public enum AssetType {
    LAPTOP,
    MONITOR,
    ACCESSORY
}
